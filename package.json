{
  "name": "project_SIP",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npm-run-all --parallel start:backend start:frontend",
    "start:backend": "cd backend && npm start",
    "start:frontend": "cd frondend && yarn serve"
  },
  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
}
